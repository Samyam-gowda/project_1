
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BUY PROD</title>
    <style>
        /* Add some basic styling to make it look neat */
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            background-color: #f5f5f5; /* added a light gray background */
        }
        img {
            margin: 10px;
            border: 1px solid #ddd;
            border-radius: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* added a subtle shadow */
        }
        h2, h3 {
            margin: 10px 0;
        }
        h2 {
            color: #333;
            font-weight: bold; /* made the product name bold */
        }
        h3 {
            color: #666;
        }
        .product {
            margin-bottom: 20px;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            background-color: #fff; /* added a white background to the product container */
        }
        .contents{
            display: flex;
            flex-wrap: wrap; /* added flex-wrap to make it responsive */
            justify-content: space-around;
        }
        .products{
            border: solid black 1px;
            border-radius: 25px;
            margin: 20px; /* added some margin to separate the products */
            width: 250px; /* set a fixed width for the product container */
            background-color: rgb(154, 228, 90);
        }
        img{
            height: 200px;
            width: 200px;
            object-fit: cover; /* made the image cover the entire container */
            border-radius: 10px; /* added a rounded corner to the image */
        }
        /* added some hover effects to make it more interactive */
        .products:hover {
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.3);
            transform: scale(1.05);
        }
        .products:hover img {
            transform: scale(1.1);
        }
    </style>
</head>
<body>
    <h1>PRODUCTS in CART ARE</h1>
<div class="contents">
    <% let total = 0 %>

    <% for( let i = 0 ;i < allProducts.length ; i++) { %>
    <div class="products">
    <img src="\image\\<%=allProducts[i].imglink%>" alt="product image">
    <h2>PRODUCT NAME:<%=allProducts[i].prodname%></h2><br>
    <h3>PRICE:<%=allProducts[i].price%></h3><br>
    <h3>LOCATION:<%=allProducts[i].location%></h3><br>
    <h3>CONTACT DETAILS:<%=allProducts[i].contact%></h3><br>
    <% total = total + allProducts[i].price %>
    <form action="/home/cart/delete/<%=allProducts[i]._id%>?_method=DELETE" method="post">
        <button type="submit"> DELETE</button>
    </form>
    </div>
    <% } %>
</div>
<h2>TOTAL BILLING AMOUNT :<%= total %></h2>
</body>
</html>